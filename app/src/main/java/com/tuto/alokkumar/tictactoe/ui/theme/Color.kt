package com.tuto.alokkumar.tictactoe.ui.theme

import androidx.compose.ui.graphics.Color

/** Predefined global colors. */
val Black = Color.Black
val White = Color.White

/** Color palette definitions for dark theme player 'X'. */
val SecondaryDark = Color(0xFFE57373)
/** Color palette definitions for light theme player 'X'. */
val SecondaryLight = Color(0xFFE57373)

/** Color palette definitions for dark theme player 'O'. */
val TertiaryDark = Color(0xFF64B5F6)
/** Color palette definitions for light theme player 'O'. */
val TertiaryLight = Color(0xFF64B5F6)

/** App background color for dark mode. */
val BackgroundDark = Color(0xFF121013)
/** App background color for light mode. */
val BackgroundLight = Color(0xFFF5F5F5)

/** Primary text/icon color for dark mode. */
val ForegroundDark = Color(0xFFE0DDE1)
/** Primary text/icon color for light mode. */
val ForegroundLight = Color(0xFF121013)

/** Container surface color for dark mode. */
val SurfaceDark = Color(0xFF1E1B20)
/** Container surface color for light mode. */
val SurfaceLight = Color(0xDDF0F0F0)

/** Text color on top of surface layers in dark mode. */
val OnSurfaceDark = Color(0xFFE0DDE1)
/** Text color on top of surface layers in light mode. */
val OnSurfaceLight = Color(0xFF121013)
